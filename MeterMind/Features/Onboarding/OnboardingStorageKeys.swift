import Foundation

/// UserDefaults keys used for onboarding persistence.
enum OnboardingStorageKeys {
    static let hasCompletedOnboarding = "hasCompletedOnboarding"
    static let lastSeenOnboardingVersion = "lastSeenOnboardingVersion"
    static let currentOnboardingVersion = 1
}
